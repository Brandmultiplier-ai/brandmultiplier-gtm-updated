export const BRAIN_EXPERIMENTS_ENABLED = false;

export function brainExperimentsDisabledMessage() {
  return "Brain experiments are paused. Campaign copy is now controlled directly from campaign settings.";
}
